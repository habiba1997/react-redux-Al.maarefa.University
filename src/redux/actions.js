import * as actions from './actionTypes';

export const flipPageBackward = () => ({
	type: actions.FLIP_PAGE_Backward,
  // payload: {
  //   
  // }
});
export const flipPageForward = () => ({
	type: actions.FLIP_PAGE_Forward,
});
export const pageNumber = () => ({
	type: actions.PAGE_NUMBER,
});
export const retrieveZeroPage = () => ({
	type: actions.RETRIEVE_ZERO_PAGE,
});
export const arrayLength = () => ({
	type: actions.RCORDS_LENGTH,
});


